<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.base.mapper.OrderMapper">
	<resultMap id="BaseResultMap" type="com.base.model.Order">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="uid" property="uid" jdbcType="INTEGER" />
		<result column="sid" property="sid" jdbcType="INTEGER" />
		<result column="deliveryaddressid" property="deliveryaddressid"
			jdbcType="INTEGER" />
		<result column="createtime" property="createtime" jdbcType="VARCHAR" />
		<result column="statu" property="statu" jdbcType="VARCHAR" />
	</resultMap>

	<select id="getOrderByBusId" resultType="com.base.model.Order">
		select s.id as
		shopId,s.name as shopname,s.price as price,s.discountprice as
		discountPirce,s.typeid as typeId,
		s.img as shopImg,o.* FROM
		xsxteam.shop s right join xsxteam.order o on
		o.sid=s.id where
		s.userid=#{busId,jdbcType=INTEGER} order by o.createtime desc
	</select>

	<select id="getShopByUserId" resultType="com.base.model.OrderCustom">
		select s.id as
		shopId,s.name as shopname,s.price as price,
		s.discountprice as
		discountPirce,s.typeid as typeId,
		s.img as shopImg,o.* FROM
		xsxteam.order o left join xsxteam.shop s on o.sid=s.id
		where
		o.uid=#{userId,jdbcType=INTEGER} order by o.createtime desc
	</select>

</mapper>